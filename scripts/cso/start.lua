
-- hint for CSO which custom surface script to load.
-- the uri is relative to the ardour scripts/cso directory.
-- only one uri_relative is possible.

function cso_custom_script_uri() return
{{
	uri_relative="null/null.lua"
}}
end

-- EOF
